# Internal reference docs — kept locally, not published to GitHub
CHANGELOG.md
COPILOT_ONBOARDING.md
MAP_NOTES.md
NOTES_1271_expansion.md
NOTES_economy_radial.md

# --- build & playtest artifacts — keep in the working folder, never publish ---
# (Fadak's repo-hygiene pass; Zaiton 7/17: comments moved to their own lines —
#  a trailing "#" is NOT a comment in gitignore, so these patterns never matched)

# versioned DEV playtest builds
Silk_Road_v*.html

# diagnostic / preview / _tmp renders (NOT the named game chart assets)
Maps/_*.png
Maps/__pycache__/
__pycache__/
*.pyc
Maps/*.npy
Maps/*.npz
Maps/work/

# Large baked master rasters (~220 MB of build intermediates). Uncomment to exclude:
# Maps/master1271/
# Maps/master_amber/
